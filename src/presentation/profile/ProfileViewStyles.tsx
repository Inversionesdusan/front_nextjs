import { SxProps, Theme } from "@mui/material";

export const styles = () => {
  const container: SxProps<Theme> = {
    backgroundColor: "red",
    width: "100%",
    height: "100px",
  };
  return {
    container,
  };
};
